export type PushFormType = {
    pushName: string,
    textTitle: string,
    mainText: string,
    language: string,
    iconLink: string,
    imgLink: string,
    messageType: string,
    audience: string
}